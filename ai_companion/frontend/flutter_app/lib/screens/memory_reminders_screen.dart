import 'package:flutter/material.dart';

class MemoryRemindersScreen extends StatelessWidget {
  const MemoryRemindersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Memory + reminders dashboard')),
    );
  }
}

